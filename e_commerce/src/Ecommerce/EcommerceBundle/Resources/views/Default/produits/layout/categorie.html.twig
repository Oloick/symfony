{% extends "::base.html.twig" %}

{% block body %}
<div id="thumbnail-preview-indicators" class="carousel slide col-sm-offset-2" data-ride="carousel">
    <div class="carousel-inner">
      <div class="item slides active">
        <div class="slide-1"></div>
      </div>
      <div class="item slides">
        <div class="slide-2"></div>
      </div>
      <div class="item slides">
        <div class="slide-3"></div>
      </div>
    </div>
    <a class="left carousel-control" href="#thumbnail-preview-indicators" role="button" data-slide="prev"><span class="glyphicon glyphicon-chevron-left"></span></a>
    <a class="right carousel-control" href="#thumbnail-preview-indicators" role="button" data-slide="next"><span class="glyphicon glyphicon-chevron-right"></span></a>
    <!-- Indicators -->
    <ol class="carousel-indicators">
      <li data-target="#thumbnail-preview-indicators" data-slide-to="0" class="active"></li>
      <li data-target="#thumbnail-preview-indicators" data-slide-to="1"></li>
      <li data-target="#thumbnail-preview-indicators" data-slide-to="2"></li>
    </ol>
</div>
</br>
        <div class="container-fluid col-sm-offset-2">
            <ul class="thumbnails row">

            {% for produit in produits %}
                <li class="col-sm-3">
                    <div class="thumbnail">
                        <img src=" {{produit.image }}" alt="{{produit.image}}" width="300" height="300">
                        <div class="caption">
                            <h4> {{ produit.nom }}</h4>
                            <p> {{produit.prix }} €</p>
                            <div class="doubleBoutonThumbnail">
                                <a class="btn btn-primary" href="{{ path('presentation' , { 'id': produit.id } )}}">Plus d'infos &nbsp;</a>
                                <a class="btn btn-success" href="{{ path('panier', {'id': produit.id} )}}">Ajouter au panier</a>
                            </div>
                        </div>
                    </div>
                </li>

            {% endfor %}
            </ul>

            <div class="row paginationRow col-sm-offset-4">
                <ul class="pagination">
                  <li><a href="#">1</a></li>
                  <li><a href="#">2</a></li>
                  <li><a href="#">3</a></li>
                  <li><a href="#">4</a></li>
                  <li><a href="#">5</a></li>
                </ul>
            </div>

        </div>
{% endblock %}
